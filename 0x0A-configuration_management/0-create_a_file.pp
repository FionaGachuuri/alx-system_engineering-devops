# This puppet manifest creates a file at /tmp/school
# with a specific owner, permission and contentiii

file { '/tmp/school':
  ensure  => 'file',
  mode    => '0744',
  owner   => 'www-data',
  group   => 'www-data',
  content => 'I love Puppet',
}
